from itertools import combinations
class Solution:
    def combinationSum3(self, k: int, n: int) -> List[List[int]]:
        lst=[1,2,3,4,5,6,7,8,9]
        return [pair for pair in combinations(lst, k) if sum(pair) == n]
